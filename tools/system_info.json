{
  "name": "system_info",
  "display_name": "System Info",
  "description": "Retrieve system information from the host machine. Returns structured JSON data about the requested category: cpu, memory, disk, network, os, or all.",
  "version": "1.1.0",
  "session_type": "one-shot",
  "llm_context": "stateless",
  "executor": "shell",
  "expose_to_agents": true,
  "agent_safe": true,

  "parameters": {
    "type": "object",
    "properties": {
      "category": {
        "type": "string",
        "enum": ["cpu", "memory", "disk", "network", "os", "all"],
        "description": "The category of system information to retrieve. 'cpu' for processor info, 'memory' for RAM usage, 'disk' for filesystem usage, 'network' for network interfaces, 'os' for OS/kernel info, 'all' for everything."
      }
    },
    "required": ["category"]
  },

  "executor_config": {
    "shell": {
      "command_template": "{rcflow} system-info {category}",
      "capture_stderr": true,
      "stream_output": false
    }
  }
}
